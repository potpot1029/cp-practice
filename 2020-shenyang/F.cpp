#include "bits/stdc++.h"
using namespace std;

int main(void) {
  ios::sync_with_stdio(false);
  int n;
  cin >> n;
  vector<int> a(n), sort_a(n);
  for (int i=0; i<n; i++) {
    cin >> a[i];
    sort_a[i] = a[i];
  }

  
  return 0;
}
